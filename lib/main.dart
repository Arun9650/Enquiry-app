import 'package:flutter/material.dart';
import 'src/app.dart'; // Import the app file

void main() {
  runApp(MyApp(
    isLoggedIn: false, // Provide a default value or a variable
    enquiries: [], // Provide a default value or a variable
  ));
}
